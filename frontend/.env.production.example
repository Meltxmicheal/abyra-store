# ============================================================
# ABYRA STORE — Frontend Production Environment Variables
# ============================================================
# DO NOT commit .env.production to git (already in .gitignore).
# Set these as Environment Variables inside the Vercel dashboard.
# Vercel automatically uses these for production builds.
# ============================================================

# ─── Supabase (Public — safe to expose) ─────────────────────
VITE_SUPABASE_URL=https://hgayhvaskddmcetltkca.supabase.co
VITE_SUPABASE_ANON_KEY=<your-supabase-anon-key>
VITE_SUPABASE_PROJECT_REF=hgayhvaskddmcetltkca

# ─── Razorpay (Public key only — safe to expose) ────────────
VITE_RAZORPAY_KEY_ID=rzp_test_SlAlrOvj6A2XAR

# ─── Backend API URL (your Render service URL) ───────────────
# Replace with your actual Render service URL — NO trailing slash
VITE_BACKEND_URL=https://abyra-backend.onrender.com

# ─── Cloudinary (Public — safe to expose) ───────────────────
VITE_CLOUDINARY_CLOUD_NAME=dze1d3uen
VITE_CLOUDINARY_UPLOAD_PRESET=ABYRA PRODUCTS
